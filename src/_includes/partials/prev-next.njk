{# there has to be a better way to do this #}
{% if collections['sleep debtor'] %}
	{% set nextPost = collections['sleep debtor'] | getNextCollectionItem(page) %}
	{% set previousPost = collections['sleep debtor'] | getPreviousCollectionItem(page) %}
	{% if nextPost or previousPost %}
	<ul>
  		{% if nextPost %}<li>Next: <a href="{{ nextPost.url | url }}">{{ nextPost.data.title }}</a></li>{% endif %}
  		{% if previousPost %}<li>Previous: <a href="{{ previousPost.url | url }}">{{ previousPost.data.title }}</a></li>{% endif %}
  	</ul>
{% endif %}
{% endif %}

{% if collections['parts inside'] %}
	{% set nextPost = collections['parts inside'] | getNextCollectionItem(page) %}
	{% set previousPost = collections['parts inside'] | getPreviousCollectionItem(page) %}
	{% if nextPost or previousPost %}
	<ul>
  		{% if nextPost %}<li>Next: <a href="{{ nextPost.url | url }}">{{ nextPost.data.title }}</a></li>{% endif %}
  		{% if previousPost %}<li>Previous: <a href="{{ previousPost.url | url }}">{{ previousPost.data.title }}</a></li>{% endif %}
  	</ul>
{% endif %}
{% endif %}

{% if collections.analog %}
	{% set nextPost = collections.analog | getNextCollectionItem(page) %}
	{% set previousPost = collections.analog | getPreviousCollectionItem(page) %}
	{% if nextPost or previousPost %}
	<ul>
  		{% if nextPost %}<li>Next: <a href="{{ nextPost.url | url }}">{{ nextPost.data.title }}</a></li>{% endif %}
  		{% if previousPost %}<li>Previous: <a href="{{ previousPost.url | url }}">{{ previousPost.data.title }}</a></li>{% endif %}
  	</ul>
{% endif %}
{% endif %}

{% if collections.digital %}
	{% set nextPost = collections.digital | getNextCollectionItem(page) %}
	{% set previousPost = collections.digital | getPreviousCollectionItem(page) %}
	{% if nextPost or previousPost %}
	<ul>
  		{% if nextPost %}<li>Next: <a href="{{ nextPost.url | url }}">{{ nextPost.data.title }}</a></li>{% endif %}
  		{% if previousPost %}<li>Previous: <a href="{{ previousPost.url | url }}">{{ previousPost.data.title }}</a></li>{% endif %}
  	</ul>
{% endif %}
{% endif %}

{% if collections.projects %}
	{% set nextPost = collections.projects | getNextCollectionItem(page) %}
	{% set previousPost = collections.projects | getPreviousCollectionItem(page) %}
	{% if nextPost or previousPost %}
	<ul>
  		{% if nextPost %}<li>Next: <a href="{{ nextPost.url | url }}">{{ nextPost.data.title }}</a></li>{% endif %}
  		{% if previousPost %}<li>Previous: <a href="{{ previousPost.url | url }}">{{ previousPost.data.title }}</a></li>{% endif %}
  	</ul>
{% endif %}
{% endif %}